// Um jogo consiste em se retirar duas fichas de um saco contendo fichas brancas e pretas. 
// Dependendo da combinação de cores das fichas retiradas, o jogador será pago na seguinte proporção: 
// Primeira Ficha Segunda Ficha Rateio 
// Branca Branca 0 
// Branca Preta 1/2 
// Preta Branca 1 
// Preta Preta 2 
// Ou seja, com duas fichas brancas o jogador perde tudo, com uma branca e uma preta recebe 
// metade do que apostou, com um preta e uma branca recebe seu dinheiro de volta e com duas pretas 
// recebe o dobro. Elaborar um algoritmo que sorteie as cores das duas fichas, o valor da aposta 
// (limitado a R$ 300,00) e calcule e informe o valor a ser recebido por cada apostador. Considere a 
// existência da quantidade de apostadores entre 1 e 200 pessoas (quantidade deve ser fornecida pelo 
// usuário). Use a função pré-definida Rand(N), que sorteia um número inteiro entre 0 e N. 

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {

    
  

    return 0;
}